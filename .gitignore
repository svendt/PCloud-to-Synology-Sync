# =============================================================================
# .gitignore — pcloud_to_synology_sync_svdt.sh
# =============================================================================

# --- Runtime log files -------------------------------------------------------
*.log

# --- Diff and missing-on-dst artefacts (runtime output — never commit) -------
# Using specific patterns instead of *.txt to avoid accidentally ignoring
# legitimate .txt files (e.g. notes, sample data).
pcloud-diff-*.txt
pcloud-missing-on-dst-*.txt
# pcloud-missing-on-src-*.txt  ← removed in v1.7 (contradicts --one-way policy)

# --- State files -------------------------------------------------------------
backup.lock
last_success
prev_missing_on_dst.txt
stats_history.txt

# --- rclone config (never commit OAuth tokens) -------------------------------
.rclone.conf
rclone.conf

# --- macOS -------------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride

# --- Editors -----------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~

# --- Temporary files ---------------------------------------------------------
*.tmp
*.bak
